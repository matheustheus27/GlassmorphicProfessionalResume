FROM node:22-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 5173

# Runs Vite with the --host flag to allow connections from outside the container
CMD ["npm", "run", "dev", "--", "--host"]